<openerp>
  <data>
    <!--Email template -->
    <!-- <record id="email_template_edi_sale_modified" model="mail.template">
        <field name="name">Sales Order - Send by Email</field>
        <field name="email_from">${(object.user_id.email and '%s &lt;%s&gt;' % (object.user_id.name, object.user_id.email) or '')|safe}</field>
        <field name="subject">${object.company_id.name} ${object.state in ('draft', 'sent') and 'Presupuesto' or 'Pedido'} (Ref ${object.name or 'n/a' })</field>
        <field name="partner_to">${object.partner_invoice_id.id}</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="auto_delete" eval="True"/>
        <field name="report_template" ref="sale.report_sale_order"/>
        <field name="report_name">${(object.name or '').replace('/','_')}_${object.state == 'draft' and 'draft' or ''}</field>
        <field name="lang">${object.partner_id.lang}</field>
        <field name="body_html"><![CDATA[
            <div style="font-family: 'Lucida Grande', Ubuntu, Arial, Verdana, sans-serif; font-size: 12px; color: rgb(34, 34, 34); background-color: #FFF; ">

            <p>Hola ${object.partner_id.name},</p>

            <p>¡Gracias por tu compra! Tus productos estarán en camino muy pronto. Te enviaremos un correo con la información de envío y número de guía dentro de las siguientes 24 hrs. Todos los detalles de tu pedido están en el documento adjunto. </p>

            <p>¿Preguntas? Llámanos al 844 154 5227</p>

            <p>¡Gracias por elegir a Blancorama!</p>

            </div>
        ]]>
        </field>
    </record>
    <record id="email_template_edi_sale" model="mail.template">
        <field name="name">Sales Order - Send by Email</field>
        <field name="email_from">${(object.user_id.email and '%s &lt;%s&gt;' % (object.user_id.name, object.user_id.email) or '')|safe}</field>
        <field name="subject">${object.company_id.name} ${object.state in ('draft', 'sent') and 'Presupuesto' or 'Pedido'} (Ref ${object.name or 'n/a' })</field>
        <field name="partner_to">${object.partner_invoice_id.id}</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="auto_delete" eval="True"/>
        <field name="report_template" ref="sale.report_sale_order"/>
        <field name="report_name">${(object.name or '').replace('/','_')}_${object.state == 'draft' and 'draft' or ''}</field>
        <field name="lang">${object.partner_id.lang}</field>
        <field name="body_html"><![CDATA[
            <div style="font-family: 'Lucida Grande', Ubuntu, Arial, Verdana, sans-serif; font-size: 12px; color: rgb(34, 34, 34); background-color: #FFF; ">

            <p>Hola ${object.partner_id.name},</p>

            <p>¡Gracias por tu compra! Tus productos estarán en camino muy pronto. Te enviaremos un correo con la información de envío y número de guía dentro de las siguientes 24 hrs. Todos los detalles de tu pedido están en el documento adjunto. </p>

            <p>¿Preguntas? Llámanos al 844 154 5227</p>

            <p>¡Gracias por elegir a Blancorama!</p>

            </div>
        ]]>
        </field>
    </record> -->
  </data>
</openerp>